meta {
  name: 알림 읽음 처리
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/users/me/notifications/{{notificationId}}/read
  body: none
  auth: bearer
}

headers {
  X-Platform: {{platform}}
}

auth:bearer {
  token: {{accessToken}}
}
