meta {
  name: 일정 삭제
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/calendar-events/{{eventId}}
  body: none
  auth: bearer
}

headers {
  X-Platform: {{platform}}
}

auth:bearer {
  token: {{accessToken}}
}
