meta {
  name: 프로필 수정
  type: http
  seq: 3
}
patch {
  url: {{baseUrl}}/users/me/profile
  body: json
  auth: bearer
}
headers {
  X-Platform: {{platform}}
  Content-Type: application/json
}
auth:bearer {
  token: {{accessToken}}
}
body:json {
  { "nickname": "로딩" }
}
